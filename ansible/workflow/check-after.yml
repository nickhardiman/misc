---
# This playbook prints a simple debug message
- name: Check after doing a thing
  hosts: all

  tasks:
    - name: Print debug message
      ansible.builtin.debug:
        msg: Check after doing a thing
...
